import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({

    textContainer: {
        flex: 1, 
      },
      
      imageContainer: {
        // flex: 1, 
        // alignItems: 'flex-end', 
      },

      weatherImage: {
        width: wp(5), 
        height: wp(5), 
        resizeMode: 'contain', 
     
      }, 
});

export default styles;

  